# Core runtime dependencies for pitchdeck-scorer
pydantic>=2.0,<3
jinja2>=3.1,<4
beautifulsoup4>=4.12,<5
httpx>=0.27,<1
tenacity>=8.2,<9

# Optional extras (install via `pip install pitchdeck-scorer[llm]` / `[crawl]` / `[dev]`):
#   llm    : openai>=1.40,<2     (LLM-augmented scoring)
#   crawl  : crawl4ai>=0.3       (richer knowledge_updater crawling)
#   dev    : pytest, pytest-cov, ruff
